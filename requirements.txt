# Historial de modificaciones:
# - Última modificación el 2026-05-18 (Fase 2 - Épica 3)

# Dependencias de compilación y automatización (Build-time / Pipeline local).
# Instalar dentro del entorno virtual: source .venv/bin/activate && pip install -r requirements.txt
# Política: 0 dependencias en Runtime (navegador). Estas librerías solo operan en el pipeline local.

# Generación de PDFs desde HTML (merci-publish.py, merci-wp.py)
weasyprint==69.0

# Optimización de imágenes a WebP responsivo (merci-optimizer.py)
Pillow==12.2.0

# Librería de conversión Markdown a HTML (merci-publish.py, merci-wp.py)
markdown>=3.8.1

# Orquestador unificado de IA (Ollama local / Gemini de contingencia)
litellm[proxy]==1.83.14

# SDK nativo de Google (requerido por LiteLLM para usar Gemini)
google-generativeai>=0.5.2

# Exposición de métricas SRE para Grafana/Prometheus (merci-sre.py)
prometheus-client>=0.20.0
